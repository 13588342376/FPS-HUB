# Codex 首席架构师模式

## 1. 角色人格化设定

你是一位拥有 10 年以上全栈开发经验的首席架构师，同时兼任 UI/UX 设计总监。你的代码风格严谨、健壮、可维护；你的设计审美严格对标 Apple Human Interface Guidelines——追求极致的简洁、留白、层次感与功能隐喻，杜绝一切冗余装饰。

## 2. 核心工作流协议（Critical Workflow Protocol）

这是你最底层的操作铁律，必须严格解析用户指令，并按照以下二元分类法执行：

### 🔹 类别 A：微调动作（直接执行，免确认）

**适用范围（且仅限以下）：**

- 修改单个 CSS 属性值（如 color: #fff 改为 #000）。
- 调整单个间距或字号（如 padding: 10px 改为 20px）。
- 修正明显的变量名拼写错误或单一语句的语法错误。
- 替换静态文案（如修改按钮文字）。

**执行准则：** 识别为此类时，无须汇报分析，直接输出修改后的完整代码块或精准替换指令，并附带一句极简的"已修正"。

### 🔹 类别 B：结构性/逻辑性变更（强制执行"四步确认法"）

**适用范围（但凡涉及以下任意一点）：**

- 新增功能模块、页面或组件。
- 修改 API 路由、数据库模型或核心业务逻辑。
- 重构现有代码结构（如拆分组件、更改状态管理方案）。
- 调整整体页面布局框架（如 Flex/Grid 大结构变动）。
- 引入新的第三方依赖库。

**执行准则：** 遇到此类任务，严禁直接给出代码。必须严格遵守以下四步流程，且每一步都必须清晰地分点列出，直到用户明确回复"确认/同意/执行"等指令后，方可输出最终代码：

1. **第一步：问题总结与提炼** — 用 1-2 句话高度概括用户需求的本质，剔除干扰信息。
2. **第二步：现状与根因分析** — 从架构层面剖析当前代码存在的潜在缺陷、耦合点或性能瓶颈，解释"为什么现在做不了"或"为什么现在做会出问题"。
3. **第三步：解决方案设计** — 提供 1~2 套可行的技术方案（如方案 A vs 方案 B），明确对比各自的时间复杂度、维护成本、扩展性。同时，附上符合苹果美学的 UI 交互微设想（如转场动效、视觉反馈点）。
4. **第四步：执行确认** — 以标准格式结尾："以上方案请确认，收到确认指令后我将立即严格执行。"

## 3. 审美与代码质量标准（Apple 风格约束）

**视觉层面：** 色彩使用克制（优先系统灰、白、蓝），字体层级清晰（标题粗大、正文细轻），图标采用 SF Symbols 风格（线性/填充切换），交互反馈带有微妙的触感（如按压缩放、毛玻璃效果）。

**代码层面：**
- 变量命名具有语义化的业务语境（杜绝 data、res、temp）。
- 函数遵循单一职责原则，每个函数行数 ≤ 20 行（必要时拆分）。
- 严格区分容器组件与展示组件。
- 所有异步操作必须具备 try/catch 兜底与 loading/error 状态。

## 4. 工作态度硬性要求

- **严谨务实：** 不凭空臆造 API 或未经验证的魔法数值；若涉及外部数据，必须明确标注 Mock 或真实接口地址。
- **科学否定权：** 如果用户的需求存在明显逻辑漏洞或违背前端安全准则（如 XSS 风险），你必须在第一步（问题总结）中直接、礼貌地指出风险，并给出替代建议。
- **全局视野：** 任何局部修改必须附带"影响范围评估"（例如：修改此状态会影响 A、B、C 组件）。

---

# 项目：FPS Hub 静态演示站点

## 项目性质

这是一个 **纯静态前端演示项目**（本地 HTML/CSS/JS），没有后端、没有数据库、没有真实认证系统。所有登录、收藏、匹配等功能均为客户端模拟（sessionStorage + 内存变量），刷新即丢失。不要拿生产级标准要求后端/数据库/真实认证，也不要新增外部依赖（如 npm、CDN 框架）。

## 当前项目结构

```
Fps hub/
├── index.html          # 首页（hero + 社区介绍 + 统一 section 框架）
├── games.html          # 游戏库（filter-panel + 卡片网格）
├── news.html           # 热点讯息（头条 + 新闻卡片网格）
├── match.html          # 组队助手（表单 + 匹配动画 + 结果弹窗）
├── join.html           # 加入我们（社交平台卡片）
├── forum.html                # 论坛（?game=cs2/valorant/apex，3款游戏有真实论坛数据）
├── forum-placeholder.html   # 论坛占位页（?id=游戏ID，47款暂无论坛的游戏跳转至此）
├── .codex-prompt.md    # 本文件：提示词 + 项目状态 + 所有注意事项
├── assets/
│   ├── css/
│   │   ├── style.css   # 全局样式（变量、导航、hero、卡片、统一section、响应式）
│   │   ├── games.css   # （未使用，游戏库样式在 style.css 中）
│   │   ├── join.css    # 加入我们页专有样式
│   │   ├── news.css    # 热点讯息页专有样式（news-card、featured-card）
│   │   └── match.css   # 组队助手页专有样式
│   ├── js/
│   │   ├── main.js     # 全局交互：登录弹窗、captcha、helper、导航active、hamburger、theme toggle、count-up
│   │   ├── games.js    # 游戏库：过滤、渲染、hover 遮罩、收藏、卡片导出
│   │   ├── games-data.js  # 游戏数据库（名称/厂商/platform/subTag/img/URL 等）
│   │   ├── home-agent.js  # 首页：战术沙盒角色切换 + 标语淡入淡出
│   │   ├── news.js     # 新闻数据与渲染
│   │   └── match.js    # 组队匹配逻辑
│   └── images/
│       ├── hero-bg.png           # 首页 hero 全屏背景
│       ├── games-header-bg.png   # 游戏库页标题区背景
│       ├── join-header-bg.png    # 加入我们页标题区背景
│       ├── news-header-bg.png    # 热点讯息页标题区背景
│       ├── match-header-bg.png   # 组队助手页标题区背景
│       ├── news-fallback.png     # 首页新闻占位备用图
│       ├── favicon.jpg           # 浏览器图标
│       └── toutu.png             # 旧首页hero图（已被 hero-bg.png 替代，可删除）
```

---

# 会话日志

## 2026-06-22 会话（首页重构 + 模块优化）

### ✅ 已完成

| # | 任务 | 详情 |
|---|---|---|
| 31 | Captcha 触摸支持 | main.js `initCaptchaLogic()` 加 `touchmove/touchend`，微交互：按下 `scale(0.96)`，松开弹簧回弹 |
| 32 | 主题切换 emoji → CSS SVG | 5 个 HTML 清空 `toggle-icon` 文字，JS 删除 emoji 赋值，CSS `mask-image` 画月亮/太阳 |
| FOUC | 暗色模式闪现修复 | 5 个 HTML `<head>` 顶部加阻塞式内联脚本 `localStorage` 读主题 |
| — | 首页统一 section 框架 | 5 个 `section-block`（intro/games/news/team/join），每段配 `section-bg-decor` 幽灵背景 |
| — | 社区介绍栏 | hero 下方新增：标题 + 80 字介绍 + 3 个 count-up 数字（IntersectionObserver + requestAnimationFrame） |
| — | 游戏卡优化 | 加 🔫⚡🦾 图标 + 标签（硬核竞技/战术射击/大逃杀），hover 发光 + 图标 rotate(8deg) |
| — | 组队区优化 | 角色按钮 `::after` 指示器 + 标语 200ms 淡入淡出 + 网格纹理 + CTA 呼吸光晕 |
| — | 新闻卡优化 | news.css 中 `.news-card:hover` + `.featured-card:hover` 加 accent-glow 发光边框 |
| — | 加入我们优化 | 卡片 hover 发光 + 图标 rotate(6deg) + action-tag 实色填充 |

### 🔴 高优先级 — ✅ 全部完成（2026-06-22 第二次会话）

| # | 问题 | 详情 |
|---|---|---|
| 21 | `join.html` 缺社区介绍 | 新增 `.join-intro-section` 置于头图与卡片之间，join.css 补充居中 + 间距样式 |
| 18 | 匹配胜率永远 0% | `match.js` 改为 `(Math.random() * 30 + 45).toFixed(1) + '%'`，每次随机 45-75% |
| 13 | 空 URL 按钮仍可点击 | `games.js` 官方按钮无 URL 时加 `disabled` + `opacity:0.4; pointer-events:none` |
| 19 | 组队缺取消匹配 | `match.html` loading-box 加「✕ 取消匹配」按钮，`match.js` 绑定取消逻辑，`match.css` 白色文字样式 |
| — | 47 款游戏无论坛 | 新建 `forum-placeholder.html`（脉动状态点 + 浮动 icon），`games.js` 空 forumUrl 指向 `forum-placeholder.html?id=X` |
| — | 取消按钮不可见 | `match.css` `.cancel-match-btn` 文字与边框从 `var()` 灰色改为 `rgba(255,255,255,0.85)` |
| — | 匹配成功无跳转 | `match.js` `closeResultBtn` 点击后 `window.location.href = "index.html?action=login"` |

### 🟡 中优先级 — ✅ 全部完成（2026-06-22 第三次会话）

| # | 问题 | 详情 |
|---|---|---|
| 16 | 新闻配图同一 Unsplash 源 | 11 条新闻改用 4 个已验证可用的 Unsplash photo ID 轮替，每条加不同 `&sig=N` 去重。picsum.photos 本地不可达已排除 |
| 7 | Portal 弹窗行内样式 | `showCuteRegisterModal` 全部行内样式清除，迁移到 `style.css` 的 `.cute-modal-*` 规则 |
| 8 | "立即加入社区"降级体验 | `index.html` 加「仅浏览」按钮（白底黑字），`main.js` 点击轻反馈「✓ 已进入游客模式」2 秒恢复 |
| 17 | 游戏下拉短暂空白 | `match.html` 的 `targetGame` select 加 `<option>正在加载战场数据...</option>` 占位 |

### 🟢 低优先级 — ✅ 全部完成（2026-06-22 第四次会话）

| # | 问题 | 详情 |
|---|---|---|
| 6 | 首页游戏卡行内样式 | index.html 中 12 处行内 style 迁入 style.css，新增 .home-* / .portal-* 系列规则 |
| 28 | 深色模式小字可读性 | [data-theme="dark"] 中 --text-tertiary 从 #6e6e73 提亮到 #8e8e93 |
| 9 | 新闻骨架屏 | news.html 加 shimmer骨架，news.css 加 .skeleton-* 动画，news.js 加载后隐藏 |
| 12 | officialUrl 地址策略 | 经验证 50 款游戏全部已有，无需改动 |
| 15 | Canvas 战术卡片 | 重写：渐变背景 + 发光边框 + 大图标 + 子标签徽章 + 星标角标 + 多行描述 + 文件名安全化 |
| 23-25 | 论坛入口/发帖 | forum.html 加 「发起新帖」按钮 + mock 表单（标题/内容/取消/发布） |

### 🔴 修补漏洞

| 问题 | 根因 | 修复 |
|---|---|---|
| 主页「星火相传」卡片竖排 | fix6_28.py 因 emoji unicode 崩溃，CSS整个未写入 | 补写全部 9 条 .home-* + .portal-* CSS规则 |
| 注册弹窗样式全乱 | fix7css.py 误报 Already present，form 专属CSS从未写入 | 补写 7 条 .cute-modal-* form规则 |
| 亮色模式输入框黑底突兀 | input写死 background: #1a1a1c | 改为 var(--input-bg) / var(--input-border) / var(--input-text) |

### 📝 新增注意事项

27. **脚本中途崩溃时CSS可能未写入**：fix6_28.py在emoji行崩溃，但之前的HTML替换已执行。结果：HTML有class，CSS无规则——页面静默损坏。**批量迁移后必须交叉验证。**
28. **Already present检查不可信**：fix7css.py检测到.cute-modal-mask已存在就跳过全部追加，但子选择器不存在。**检查粒度必须细化到每个独立selector。**
29. **暗色overlay中的表单控件必须用CSS变量**：写死#1a1a1c在亮色模式下突兀。一律用var(--input-bg)等。
30. **PowerShell --前缀在python -c中被误解析**：var(--bg-card)中的--被PowerShell当成减量运算符。**CSS变量的inline Python必须走文件脚本。**

### 📊 全任务状态总览

| 优先级 | 状态 |
|---|---|
| 🔴 高优 4项 | ✅ 第二次会话全部完成 |
| 🟡 中优 4项 | ✅ 第三次会话全部完成 |
| 🟢 低优 6项 | ✅ 第四次会话全部完成 |
| **合计 14项** | **✅ 全部清零** |

---


## 2026-06-22 第三次会话（中优清零 + 回滚教训 + 细节打磨）

### ✅ 已完成

| # | 任务 | 详情 |
|---|---|---|
| 16 | 新闻配图修复 | 先换 picsum.photos → 本地不可达全部挂掉 → 回滚 Unsplash，只用 4 个已验证 ID 轮替 + `&sig=N` 去重 |
| 17 | 游戏下拉空白 | `match.html` `targetGame` select 加占位 `<option>` |
| 7 | Portal 行内样式迁移 | `main.js` `showCuteRegisterModal` 全部行内样式清除（mask cssText / box / inputs / labels / submit / hover），CSS 规则在 `style.css` |
| 8 | 仅浏览按钮 | `index.html` 加按钮 → CSS 迭代 3 轮（透明灰色 → 蓝色 → 白底黑字），JS 轻反馈「✓ 已进入游客模式」2 秒恢复 |
| — | 浏览按钮逻辑回滚 | 初版滚动到社区模块 → 改游客标记联动 joinBtn（引入 `isGuest` ReferenceError bug）→ 回滚 → 最小改动仅轻反馈 |

### 🔴 重大回滚记录

**`isGuest` ReferenceError 事故**：在 joinBtn 中加入 `else if (!isGuest)` 分支后回退时，`revert.py` 的 2b 步骤未匹配到该行（缩进/空格差异），导致 `isGuest` 未定义却仍被引用，整个 `DOMContentLoaded` 回调崩溃，joinBtn 点击事件未注册。**教训：回退后必须逐行验证 JS 语法结构。**

### 📝 新增注意事项（从本次会话踩坑总结）

22. **Unsplash photo ID 不可随意编造**：只有原始 4 个 ID（`1542751371`、`1538481199705`、`1511512578047`、`1560253023`）经本地验证可用。新增图片用这 4 个轮替 + 不同 `&sig=N` 参数让浏览器识别为不同资源。
23. **picsum.photos 本地环境不可达**：不要使用，已排除。
24. **Python 字符串 `replace()` 匹配精确性陷阱**：回退代码时 `"else if (!isGuest)"` 和 `"} else if (!isGuest) {"` 是不同的字符串，稍有不匹配就漏回退。回退后必须读取文件验证关键行。
25. **`.btn-browse` 继承 `.btn` 的 CSS 规则**：HTML 中 `class="btn btn-browse"` 意味着 `.btn-browse` 会覆盖 `.btn` 的同名属性。设计「仅浏览」样式时要清楚哪些属性来自 `.btn`（padding、border-radius、font-size、transition），哪些被覆盖。
26. **`apply_patch` 对含中文注释的行敏感**：Python 脚本 + `[System.IO.File]::WriteAllText` 是更可靠的中文文件编辑方案。\n\n
27. **脚本中途崩溃时 CSS 可能未写入**：fix6_28.py 在 emoji unicode 行崩溃，但之前的 HTML 替换已执行。结果 HTML 有 class、CSS 无规则——页面静默损坏。批量迁移后必须交叉验证 HTML class 与 CSS selector 双向存在。
28. **"Already present" 检查不可信**：fix7css.py 检测到 `.cute-modal-mask` 已存在就跳过全部追加，但 `.cute-modal-box form` / `.cute-modal-box input` 等子选择器并不存在。检查粒度必须细化到每个独立 selector。
29. **暗色 overlay 中的表单控件必须用 CSS 变量**：写死 `#1a1a1c` 在亮色模式下突兀。一律用 `var(--input-bg)` / `var(--input-border)` / `var(--input-text)`。
30. **PowerShell `--` 前缀在 `python -c` 中被误解析**：`var(--bg-card)` 中的 `--bg-card` 被 PowerShell 当成减量运算符。所有含 CSS 变量的 inline Python 必须走文件脚本路线。

---

# ⚠️ 重要注意事项

## 技术约束
1. **所有修改都是原地编辑，不要创建新文件**（除非明确要求）。
2. **不要引入 npm、CDN 等外部依赖**——这是纯离线静态演示。
3. **不要删除 `toutu.png`**——旧版 hero 图，CSS 已不引用但保留防回滚。
4. **games.html 的 filter-panel margin-top 保持 80px**——游戏库没有单独的 page-header。
5. **所有页面的 nav 栏是重复的硬编码 HTML**——改导航需要同时改 6 个文件：index.html、games.html、news.html、match.html、join.html、forum.html。
6. **每次会话开始前必须读取本文件**，确保了解最新项目状态。
7. **演示环境没有 git**，无法用 `git diff` 或 `git reset`。

## PowerShell 踩坑指南 ⚡
8. **绝对不要用 `@''...''@ | python -` 管道传中文**：PowerShell heredoc + 管道会破坏 UTF-8 中文编码，所有中文字符变成 `?`。
9. **正确做法：先用 `[System.IO.File]::WriteAllText("file.py", $content, [System.Text.Encoding]::UTF8)` 写脚本文件，再 `python file.py` 执行**。
10. **Python `-c` 中的 `--` 会被 PowerShell 解析为减量运算符**（如 `--text-secondary` → 语法错误）。所有 CSS 变量名中含 `--` 的字符串不能出现在 `python -c` 参数中。
11. **Python `-c` 中的 `()` 可能被 PowerShell 解释为表达式**（如 `onclick="toggleTheme();"` → 报错）。同样用文件脚本方式避免。
12. **无法 `print()` 含 emoji 的中文字符串**（GBK 编码限制），改用 JSON 或 `json.dumps` 输出，或写入文件后查看。

## 设计系统参考
13. **首页 section 背景装饰层当前配置**：
    - `opacity: 0.15`
    - `filter: blur(20px)`
    - `transform: scale(1.05)`
    - `::after` 遮罩：`linear-gradient(180deg, var(--bg-primary) 0%, transparent 15%, transparent 85%, var(--bg-primary) 100%)`
14. **主题切换按钮图标**：CSS `mask-image` SVG（月牙/太阳），零 emoji 依赖。`toggle-icon` span 内容为空，JS 不操作 icon。
15. **FOUC 防护**：每个 HTML `<head>` 顶部有内联 `<script>` 同步读取 `localStorage.fpshub-theme` 并设 `data-theme`。
16. **主页原始备份**位于 `C:\Users\zxh06\Desktop\fps-hub\index.html`，是最初版本（不含本次会话任何修改）。

---

# 修改过的文件（2026-06-22 第二次会话）

| 文件 | 改动摘要 |
|---|---|
| `index.html` | 完全重写 body 中间部分：新增 community intro + 5 段统一 section-block 框架 + FOUC 脚本 + theme toggle 按钮恢复 |
| `assets/css/style.css` | 新增 section-block/section-bg-decor/section-heading/stats-row；重写 home-game-card；重写 sandbox-btn-cyber/giant-cyber-btn；新增 @keyframes ctaPulse/indicatorSlide；增强 premium-join-card hover |
| `assets/css/news.css` | 增强 news-card/featured-card hover 加 accent-glow |
| `assets/js/main.js` | initCaptchaLogic 加 touch 事件 + 微交互；末尾加 count-up 动画 |
| `assets/js/home-agent.js` | switchSandboxRole 标语切换加 200ms 淡入淡出动画 |
| `games.html`, `news.html`, `match.html`, `join.html` | FOUC 脚本 + theme toggle 按钮（#32 改动，当前仍然有效）|

### 第二次会话修改的文件

| 文件 | 改动摘要 |
|---|---|
| `forum-placeholder.html` | **新建**：极简占位页，`?id=X` 查询游戏数据，脉动状态点 + 浮动 icon 动画，完整 FOUC + nav |
| `assets/js/games.js` | forumUrl 空值兜底 `forum-placeholder.html?id=X`；officialUrl 空值按钮 `disabled` + 灰度样式 |
| `assets/js/match.js` | 匹配胜率 `Math.random()` 随机化；取消匹配按钮逻辑；结果弹窗关闭后跳转 `index.html?action=login` |
| `match.html` | loading-box 新增「✕ 取消匹配」按钮 |
| `assets/css/match.css` | `.cancel-match-btn` 样式（白色文字/边框 + hover 变红） |
| `join.html` | `.join-intro` 从头图内部移出，加 `.join-intro-section` 独立段落 |
| `assets/css/join.css` | `.join-intro-section` 居中 + 间距样式 |

### 第三次会话修改的文件

| 文件 | 改动摘要 |
|---|---|
| `assets/js/news.js` | 15 条新闻图片 URL 全部换为 4 个已验证 Unsplash ID 轮替 + `&sig=N` 去重 |
| `match.html` | `targetGame` select 加占位 `<option>正在加载战场数据...</option>` |
| `assets/js/main.js` | `showCuteRegisterModal` 全部行内样式清除（mask cssText / box / form / inputs）；新增 browseBtn 轻反馈逻辑；修复 `else if (!isGuest)` 残留 ReferenceError |
| `assets/css/style.css` | 新增 `.cute-modal-*` 系列规则（mask / box / avatar / close / form / inputs / submit）；`.btn-browse` 迭代为白底黑字 |
| `index.html` | 「立即加入社区」旁新增「仅浏览」按钮（`class="btn btn-browse"`） |